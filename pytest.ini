[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    tyssue: needs the optional `tyssue` dependency
    cobra: runs a dFBA composite, needs cobra and the bundled textbook model
    bigg: needs network access to download models from the BiGG database
